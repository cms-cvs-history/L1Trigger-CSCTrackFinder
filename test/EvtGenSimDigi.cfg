process Rec = 
{
  untracked PSet maxEvents = {untracked int32 input = 1000}

  untracked PSet configurationMetadata = 
  {
    untracked string version = "$Revision: 1.11 $"
    untracked string name = "$Source: /local/projects/CMSSW/rep/CMSSW/Configuration/ReleaseValidation/data/SingleMuMinusPt10.cfg,v $"
    untracked string annotation = "RelVal single particle gun mu- 10GeV"
   }

  include "Configuration/ReleaseValidation/data/Services.cff"
  include "Configuration/StandardSequences/data/FrontierConditions.cff"
  include "FWCore/MessageService/data/MessageLogger.cfi"
    
  untracked PSet options = 
  {
    include "FWCore/Framework/test/cmsExceptionsFatalOption.cff"
    untracked bool wantSummary = true
    untracked bool makeTriggerResults = true 
  }

  source = FlatRandomPtGunSource 
  { 
    string psethack  = "single mu pt 10 neg"	
    untracked uint32 firstRun = 1
    untracked PSet PGunParameters =
    {
      untracked vint32  PartID = {13, 13}
      untracked double MinEta =  0.9
      untracked double MaxEta =  2.5
      untracked double MinPhi = -3.14159265358979323846 # in radians
      untracked double MaxPhi =  3.14159265358979323846
      untracked double MinPt  = 149.99
      untracked double MaxPt  = 150.01
    }
    untracked int32 Verbosity = 0 # set to 1 (or greater)  for printouts
  }
  
  include "Configuration/StandardSequences/data/Generator.cff"
  include "Configuration/StandardSequences/data/Reconstruction.cff"
  include "Configuration/StandardSequences/data/Simulation.cff"
  include "Configuration/StandardSequences/data/MixingNoPileUp.cff" 
  include "Configuration/StandardSequences/data/VtxSmearedGauss.cff" 
  include "Configuration/StandardSequences/data/L1Emulator.cff"
  #include "Configuration/StandardSequences/data/DigiToRaw.cff"

  path p0 = {pgen}                  # generator information
  path p1 = {psim}                  # simulation
  path p2 = {pdigi}                 # digitization

  include "Configuration/EventContent/data/EventContent.cff"

  module FEVT = PoolOutputModule 
  { 
    #using FEVTSIMEventContent
    untracked string fileName = "DoubleMuPt150.root"
	untracked vstring outputCommands = 
	{
		"keep *_*_*_*",
		"drop PCaloHits_*_*_*",
		"drop PCaloHitCrossingFrame_*_*_*",
		"drop HcalTriggerPrimitiveDigisSorted_*_*_*",
		"drop recoGenJets_*_*_*"
		
	}
  }

  endpath outpath = {FEVT}
  schedule = {p0,p1,p2,outpath}
}