process analyze = {
     untracked PSet maxEvents = {untracked int32 input = -1}

     service = MessageLogger {
       untracked vstring destinations = { "cout"  }
       untracked PSet cout = { untracked string threshold = "INFO" }
       untracked vstring debugModules   = { "*" }
     }

     source = PoolSource{
         untracked vstring fileNames = {
"file:/tmp/kkotov/test.root"
}
     }

#     include "EventFilter/CSCTFRawToDigi/data/csctfunpacker.cfi"

     module csctfanalyzer = CSCTFanalyzer {
         untracked InputTag cscTriggerPrimitiveDigis = cscTriggerPrimitiveDigis:
         untracked InputTag trackProducer            = csctfTrackDigis:
     }

#     path p = {cscTrackFinderDigis,csctfanalyzer}
     path p = {csctfanalyzer}
}
